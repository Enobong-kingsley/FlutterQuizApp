/* 
NOTES:
1. Final vs Const :
final is a runtime constant value as final values never changed and is runtime 
The only difference between the final and const keyword is that final is a runtime-constant, which in turn means that its value can be assigned at runtime instead of the compile-time that we had for the const keywo



*/